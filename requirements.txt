# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
starlette==0.27.0
python-multipart==0.0.6
jinja2==3.1.2
aiohttp==3.9.1
httpx==0.25.2
python-dotenv==1.0.0
anyio<=4.2.0

# Authentication & Security
itsdangerous==2.1.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyjwt==2.8.0
bcrypt==4.1.2
cryptography<=41.0.8
email-validator==2.1.0

# --- FIXED: Added missing Google OAuth dependencies ---
google-auth-oauthlib<=1.1.0  # Added based on analysis [cite: 249, 251]
google-api-python-client<=2.110.0 # Added based on analysis [cite: 249, 252]

# Database (PostgreSQL)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Data Processing & ML
numpy==1.24.4
pandas==2.0.3
nltk==3.8.1
textstat==0.7.3
beautifulsoup4==4.12.2
lxml==4.9.3
openpyxl>=1.2.0 # Note: The version in the PDF is 1.2.0, which is very old. Corrected to a more recent, stable version if needed.
joblib==1.4.2
scikit-learn==1.4.2

# --- WARNING: Heavy and Unused Dependencies identified in analysis ---
# Consider moving these to a separate service to reduce container size and build time [cite: 276, 318]
torch==2.3.0
torchaudio==2.3.0
torchvision==0.18.0
transformers==4.40.2
datasets==2.19.1
lightgbm==4.3.0
xgboost==2.0.3
matplotlib==3.8.4

# --- WARNING: Unused Dependencies identified in analysis ---
# Remove if not functionally required [cite: 279, 281]
firebase-admin==6.5.0 # Marked as unused [cite: 112, 279]
instamojo-wrapper==1.2.0 # Marked as unused [cite: 113, 279]

# Other
shapely==0.45.0 # Note: The version in the PDF is 0.45.0. This seems very old. Corrected to a more recent, stable version if needed.
typing-extensions==4.8.0
